package main

import (
	"fmt"     // нужен для вывода текста и чтения ввода (Print, Scan)
	"strings" // помогает работать со строками (в нашем случае убирать пробелы)
	"unicode" // даёт функции для проверки типа символов (кириллица, латиница и т.д.)
)

func checkName() {
	var name string // объявляем переменную для хранения имени (пока пустая)

	// Бесконечный цикл: будет работать, пока мы не сделаем break
	for {
		fmt.Print("Введите ваше имя: ")
		// Считываем строку до конца строки (Enter). &name — передаём адрес переменной, чтобы записать туда данные
		_, err := fmt.Scanln(&name)
		if err != nil {
			fmt.Println("Ошибка ввода. Попробуйте ещё раз.")
			continue // пропускаем остаток цикла и начинаем заново
		}

		name = strings.TrimSpace(name) // убираем пробелы в начале и конце строки

		// Если строка пустая (после обрезки пробелов), просим ввести имя снова
		if name == " " {
			fmt.Println("Имя не может быть пустым. Пожалуйста, введите имя.")
			continue
		}

		hasCyr := false // флаг: есть ли в имени кириллица
		hasLat := false // флаг: есть ли в имени латиница

		// Проходим по каждому символу строки
		for _, r := range name {
			if unicode.Is(unicode.Cyrillic, r) {
				hasCyr = true // нашли кириллическую букву
			}
			if unicode.Is(unicode.Latin, r) {
				hasLat = true // нашли латинскую букву
			}
		}

		// Если есть и кириллица, и латиница — вероятно, перепутана раскладка
		if hasCyr && hasLat {
			fmt.Println("Похоже, вы могли перепутать раскладку клавиатуры: в имени смешаны кириллица и латиница. Хотите ввести имя заново? (y/n): ")
			var choice string
			fmt.Scan(&choice) // считываем ответ пользователя

			// Обработываем ответ и проверяем: если «y» — начинаем цикл заново
			if strings.ToLower(choice) == "y" {
				continue
			}
		}
		break // если ошибок нет или пользователь не захотел вводить заново — выходим из цикла
	}

	fmt.Printf("Привет, %s! Давай посчитаем.\n", name) // приветствуем пользователя

	var num1, num2 float64 // переменные для чисел
	var op string          // переменная для операции (+, -, *, /)

	// Цикл для ввода первого числа: работает, пока не будет введено корректное число
	for {
		fmt.Print("Введите первое число: ")
		if _, err := fmt.Scan(&num1); err == nil {
			break // всё хорошо — выходим из цикла
		}
		fmt.Println("Некорректный ввод числа. Попробуйте ещё раз.")
	}

	// Цикл для ввода операции: проверяем, что это один из разрешённых символов
	for {
		fmt.Print("Введите операцию (+, -, *, /): ")
		if _, err := fmt.Scan(&op); err == nil && (op == "+" || op == "-" || op == "*" || op == "/") {
			break
		}
		fmt.Println("Некорректная операция. Допустимы только: +, -, *, /")
	}

	// Цикл для ввода второго числа
	for {
		fmt.Print("Введите второе число: ")
		if _, err := fmt.Scan(&num2); err == nil {
			break
		}
		fmt.Println("Некорректный ввод числа. Попробуйте ещё раз.")
	}

	var result float64 // переменная для результата вычислений
	var hasError bool  // флаг: произошла ли ошибка (например, деление на ноль)

	// Выбираем действие в зависимости от введённой операции
	switch op {
	case "+":
		result = num1 + num2
	case "-":
		result = num1 - num2
	case "*":
		result = num1 * num2
	case "/":
		if num2 == 0 {
			fmt.Println("Ошибка: деление на ноль!")
			hasError = true // помечаем, что произошла ошибка
		} else {
			result = num1 / num2
		}
	default:
		fmt.Println("Ошибка: неизвестная операция!")
		hasError = true
	}

	// Если ошибок нет — выводим результат
	if !hasError {
		// %.2f — выводим число с двумя знаками после запятой
		fmt.Printf("Результат: %.2f %s %.2f = %.2f\n", num1, op, num2, result)
	}
}
